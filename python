# 我的 Python 学习记录 - 第一周

## 今天学到的新知识 (2026-03-18)
今天刚刚开始了解 Python 这门语言，感觉比 C 语言简单很多。
主要学习了如何输出和定义变量：

1. 打印输出：使用了 `print("Hello World!")`
2. 变量定义：不需要像以前那样写 `int`，直接 `a = 10` 就可以了，很方便。

## 遇到的坑
Python 对缩进（空格）要求实在太严格了！今天写错了一个空格，一直报 `IndentationError` 的错误，找了半天才发现。

## 明天的计划
打算学习一下 `if-else` 条件判断和简单的 `for` 循环。